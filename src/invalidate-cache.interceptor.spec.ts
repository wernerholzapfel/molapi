import { InvalidateCacheInterceptor } from './invalidate-cache.interceptor';

describe('InvalidateCacheInterceptor', () => {
  it('should be defined', () => {
    expect(new InvalidateCacheInterceptor()).toBeDefined();
  });
});
